services:
  bimbly-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bimbly-frontend
    ports:
      - "81:80"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - bimbly-network

networks:
  bimbly-network:
    external: true
